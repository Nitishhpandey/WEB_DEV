 // && => and operator, both have to be true
 // || => or operator, only one has to be true
 // ! => not operator, reverses the boolean value

 // building an applicatin which requires to have login credentials and valid credit card to purchease a product
     
 let isLoggedIn = true;
 let ispaidUser = false;


 console.log(isLoggedIn && ispaidUser);


let isEmailuser = true;
let isGoogleuser = false;

console.log(isEmailuser || isGoogleuser);

